apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysset
  labels:
    app: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      name: mytemp
      labels:
        app: myapp
    spec:
      containers:
        - name: mycont
          image: mysql:8.0
          ports:
            - name: myport
              containerPort: 3306
          env:
            - name: MYSQL_USER
              valueFrom:
                configMapKeyRef:
                  name: myconfigmap
                  key: mysql_user

            - name: MYSQL_PASSWORD
              valueFrom: 
                configMapKeyRef:
                  name: myconfigmap
                  key: mysql_password
                
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: myconfigmap
                  key: mysql_database

            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: myconfigmap
                  key: mysql_root_password
...
---
apiVersion: v1
kind: Service
metadata:
  name: mysvc
  labels:
    app: myapp
spec:
  clusterIP: None
  selector:
    app: myapp
  ports:
    - name: myport
      protocol: TCP
      port: 3306
      targetPort: 3306
